RAG with some accounting PDFs to answer simple straight forward questions

populate database first:
python populate_database.py

To open UI use:
streamlit run UI.py

To run just 1 command use:
python query_data.py  "PROMPT"